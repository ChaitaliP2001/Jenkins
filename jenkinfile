pipeline {
     agent_any
     stages{
       stage("clean up"){
         steps{
           deleteDir()        
         }
       
       }
       stage("clone the git repo."){
          steps{
              sh "git clone "
          }
       }
     
     }
}
